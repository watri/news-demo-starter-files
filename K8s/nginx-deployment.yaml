apiVersion: apps/v1
kind: Deployment
metadata:
  name: website
  labels:
    app: websitek
spec:
  replicas: 2
  selector:
    matchLabels:
      app: website
  template:
    metadata:
      labels:
        app: website
    spec:
      containers:
      - name: website
        image: watri/website:latest
        ports:
        - containerPort: 3000